# Gemma 3 4B agentic sweep, 27 August 2026

Sixteen seeds, deterministic, silent break, from a second agentic
implementation since superseded by `explore_agent.py`. The code is in
`archive/superseded-agentic-arm/`.

Kept because it is the largest agentic sample the project has. Whether it is
comparable to the `explore_agent.py` arm is unverified: the two harnesses log
the same step fields by design, but that is an intention, not a checked fact.

Also here: earlier smoke and dry runs from the same harness, kept for
provenance.
