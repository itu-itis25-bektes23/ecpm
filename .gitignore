# Python bytecode cache
__pycache__/
*.pyc

# JetBrains/PyCharm project settings
.idea/

# macOS Finder metadata
.DS_Store

# Generated pilot run artifacts (run_pilot.py --out default);
# curated results belong in runs/, which stays tracked
pilot_artifacts/
output/

# Local secrets, if a .env is ever added instead of exporting API keys directly
.env

patches/

# Resume markers written by the run loops. Scratch state, not results.
.done
**/.done

# Finetuning arm: LoRA adapters and payload folders are regenerated,
# not committed. Adapters are ~74MB each.
anchor_adapter_*/
*.safetensors
payloads_*/
anchors.json
